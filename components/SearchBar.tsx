import { Search } from "lucide-react";

const SearchBar = () => {
  return (
    <div>
      <Search className="w-5 h-5 hover:text-pink-300 hoverEffect text-white hover:cursor-pointer" />
    </div>
  );
};
export default SearchBar;
